require_relative './config/env'
require 'sinatra/activerecord/rake' # to get all rake commands build into sinatra records

desc "Allow console to have access to Models and DB"
task :console do
  Pry.start
end
